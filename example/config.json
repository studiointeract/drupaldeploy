{
  // Server authentication info.
  "servers": [
    {
      "host": "hostname",
      "username": "root",
      "sshOptions": {
      #  "agentForward": true
      }
    }
  ],

  // Location of Drupal web root in the repository.
  "web": "web"
}
